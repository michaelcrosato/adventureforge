# Local-only credentials for the optional Linear intake mirror.
# Copy this file to .env (gitignored) and fill in the API key; never commit it.
# LINEAR_PROJECT and LINEAR_TEAM fall back to this repo's defaults when blank.
LINEAR_API_KEY=
LINEAR_PROJECT=
LINEAR_TEAM=
